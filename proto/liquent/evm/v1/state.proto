syntax = "proto3";
package liquent.evm.v1;

option go_package = "github.com/liquentlabs/liquent-core/liquent-chain/modules/evm/types";

// State represents a single Storage key value pair item.
message State {
  // key is the stored key
  string key = 1;
  // value is the stored value for the given key
  string value = 2;
}